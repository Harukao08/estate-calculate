<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>보유세 통합 계산기 - 부동산계산하기</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;500;700;900&family=Archivo:wght@600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/calculator.css">
</head>
<body>
    <!-- Header -->
    <header class="header">
        <div class="logo">
            <div class="logo-icon">부</div>
            <a href="/" style="text-decoration: none; color: inherit;">부동산계산하기</a>
        </div>
    </header>

    <!-- Main Content -->
    <main class="container" style="padding-top: 32px; padding-bottom: 80px;">
        <a href="/hold" class="back-button">
            ← 집을 가지고 있어요
        </a>

        <div class="calculator-header">
            <div class="calculator-icon-large">💰</div>
            <h1 class="calculator-title">보유세 통합 계산기</h1>
            <p class="calculator-subtitle">재산세와 종합부동산세를 한 번에 계산합니다</p>
        </div>

        <!-- 보유세 안내 -->
        <div class="info-box">
            <div class="info-title">
                💡 보유세란?
            </div>
            <div class="info-content">
                보유세는 <strong>재산세</strong>와 <strong>종합부동산세(종부세)</strong>를 합친 개념입니다.
                <br><br>
                • <strong>재산세</strong>: 모든 주택 소유자가 납부 (매년 7월, 9월)<br>
                • <strong>종부세</strong>: 공시가격 합계 6억원(1주택 12억원) 초과 시 납부 (매년 12월)
            </div>
        </div>

        <!-- 계산 입력 폼 -->
        <div class="calc-section">
            <h2 class="calc-section-title">주택 정보</h2>
            
            <div class="input-group">
                <label class="input-label">주택 공시가격</label>
                <div class="input-wrapper">
                    <input type="text" id="publicPrice" class="input-field" placeholder="예: 600000000" inputmode="numeric">
                    <span class="input-suffix">원</span>
                </div>
                <div class="input-helper">국토교통부 공시가격 (부동산공시가격알리미에서 확인)</div>
            </div>

            <div class="input-group">
                <label class="input-label">보유 주택 수</label>
                <div class="radio-group">
                    <label class="radio-item">
                        <input type="radio" name="housingCount" value="1" checked>
                        <span class="radio-label">1주택</span>
                    </label>
                    <label class="radio-item">
                        <input type="radio" name="housingCount" value="2">
                        <span class="radio-label">2주택</span>
                    </label>
                    <label class="radio-item">
                        <input type="radio" name="housingCount" value="3">
                        <span class="radio-label">3주택 이상</span>
                    </label>
                </div>
            </div>

            <div id="multiHouseSection" style="display: none;">
                <div class="input-group">
                    <label class="input-label">보유 주택 공시가격 총합</label>
                    <div class="input-wrapper">
                        <input type="text" id="totalPublicPrice" class="input-field" placeholder="예: 1500000000" inputmode="numeric">
                        <span class="input-suffix">원</span>
                    </div>
                    <div class="input-helper">보유한 모든 주택의 공시가격 합계</div>
                </div>
            </div>

            <div class="input-group">
                <label class="input-label">조정대상지역 여부</label>
                <div class="radio-group">
                    <label class="radio-item">
                        <input type="radio" name="regulated" value="no" checked>
                        <span class="radio-label">일반지역</span>
                    </label>
                    <label class="radio-item">
                        <input type="radio" name="regulated" value="yes">
                        <span class="radio-label">조정대상지역</span>
                    </label>
                </div>
            </div>

            <button class="calc-button" id="calculateButton">
                보유세 계산하기
            </button>
        </div>

        <!-- 결과 영역 -->
        <div id="resultSection" class="result-section" style="display: none;">
            <h2 class="result-title">💰 계산 결과</h2>
            
            <div class="result-card total">
                <div class="result-label">연간 총 보유세</div>
                <div class="result-value" id="totalTax">0원</div>
            </div>

            <div class="result-breakdown">
                <h3 class="breakdown-title">재산세 (7월, 9월 분납)</h3>
                <div class="breakdown-item">
                    <span class="breakdown-label">재산세 본세</span>
                    <span class="breakdown-value" id="propertyTaxBase">0원</span>
                </div>
                <div class="breakdown-item">
                    <span class="breakdown-label">도시지역분</span>
                    <span class="breakdown-value" id="urbanTax">0원</span>
                </div>
                <div class="breakdown-item">
                    <span class="breakdown-label">지방교육세</span>
                    <span class="breakdown-value" id="educationTax">0원</span>
                </div>
                <div class="breakdown-item" style="border-top: 2px solid var(--border); padding-top: 12px; margin-top: 8px;">
                    <span class="breakdown-label" style="font-weight: 700;">재산세 합계</span>
                    <span class="breakdown-value" style="font-weight: 700; color: var(--primary);" id="propertyTaxTotal">0원</span>
                </div>
            </div>

            <div class="result-breakdown" id="comprehensiveSection">
                <h3 class="breakdown-title">종합부동산세 (12월)</h3>
                <div class="breakdown-item">
                    <span class="breakdown-label">종부세 본세</span>
                    <span class="breakdown-value" id="comprehensiveTaxBase">0원</span>
                </div>
                <div class="breakdown-item">
                    <span class="breakdown-label">농어촌특별세</span>
                    <span class="breakdown-value" id="ruralTax">0원</span>
                </div>
                <div class="breakdown-item" style="border-top: 2px solid var(--border); padding-top: 12px; margin-top: 8px;">
                    <span class="breakdown-label" style="font-weight: 700;">종부세 합계</span>
                    <span class="breakdown-value" style="font-weight: 700; color: var(--primary);" id="comprehensiveTaxTotal">0원</span>
                </div>
            </div>

            <div class="result-info">
                <div class="info-icon">ℹ️</div>
                <div class="info-text" id="taxInfo"></div>
            </div>

            <div class="result-info" style="background: rgba(0, 217, 181, 0.05); border-color: rgba(0, 217, 181, 0.2);">
                <div class="info-icon">📅</div>
                <div class="info-text">
                    <strong>납부 일정</strong><br>
                    • 재산세: 7월 (1~15일), 9월 (16~30일) 분납<br>
                    • 종부세: 12월 (1~15일) 일괄 납부
                </div>
            </div>

            <div class="result-actions">
                <button class="action-button secondary" id="resetButton">
                    다시 계산하기
                </button>
                <button class="action-button primary" id="shareButton">
                    결과 공유하기
                </button>
            </div>
        </div>
    </main>

    <script>
        // DOM 로드 완료 후 실행
        document.addEventListener('DOMContentLoaded', function() {
            // 계산 버튼 이벤트
            const calculateButton = document.getElementById('calculateButton');
            if (calculateButton) {
                calculateButton.addEventListener('click', calculateHoldingTax);
            }

            // 리셋 버튼
            const resetButton = document.getElementById('resetButton');
            if (resetButton) {
                resetButton.addEventListener('click', resetCalculator);
            }

            // 공유 버튼
            const shareButton = document.getElementById('shareButton');
            if (shareButton) {
                shareButton.addEventListener('click', shareResult);
            }

            // 숫자 입력 필드 포맷팅
            ['publicPrice', 'totalPublicPrice'].forEach(id => {
                const input = document.getElementById(id);
                if (input) {
                    input.addEventListener('input', function(e) {
                        let value = e.target.value.replace(/[^0-9]/g, '');
                        e.target.value = value;
                    });

                    input.addEventListener('keypress', function(e) {
                        if (e.key === 'Enter') {
                            calculateHoldingTax();
                        }
                    });
                }
            });

            // 주택 수에 따라 총합 입력 표시
            const housingCountInputs = document.querySelectorAll('input[name="housingCount"]');
            housingCountInputs.forEach(input => {
                input.addEventListener('change', function() {
                    const multiSection = document.getElementById('multiHouseSection');
                    if (this.value !== '1') {
                        multiSection.style.display = 'block';
                    } else {
                        multiSection.style.display = 'none';
                    }
                });
            });
        });

        // 숫자 포맷팅
        function formatNumber(num) {
            return new Intl.NumberFormat('ko-KR').format(Math.floor(num));
        }

        function formatCurrency(num) {
            return formatNumber(num) + '원';
        }

        function formatKoreanCurrency(num) {
            const billion = Math.floor(num / 100000000);
            const tenThousand = Math.floor((num % 100000000) / 10000);
            const remainder = num % 10000;
            
            let result = '';
            if (billion > 0) result += billion + '억 ';
            if (tenThousand > 0) result += formatNumber(tenThousand) + '만 ';
            if (remainder > 0 && billion === 0) result += formatNumber(remainder);
            
            return result.trim() + '원';
        }

        // 보유세 계산 함수
        function calculateHoldingTax() {
            const publicPrice = parseInt(document.getElementById('publicPrice').value) || 0;
            
            if (publicPrice === 0) {
                alert('주택 공시가격을 입력해주세요.');
                return;
            }

            const housingCount = parseInt(document.querySelector('input[name="housingCount"]:checked').value);
            const regulated = document.querySelector('input[name="regulated"]:checked').value;
            
            let totalPublicPrice = publicPrice;
            if (housingCount > 1) {
                totalPublicPrice = parseInt(document.getElementById('totalPublicPrice').value) || publicPrice;
            }

            // 1. 재산세 계산
            let propertyTaxRate = 0;
            const taxableAmount = publicPrice * 0.6; // 공정시장가액비율 60%

            if (taxableAmount <= 60000000) {
                propertyTaxRate = 0.001; // 0.1%
            } else if (taxableAmount <= 150000000) {
                propertyTaxRate = 0.0015; // 0.15%
            } else if (taxableAmount <= 300000000) {
                propertyTaxRate = 0.0025; // 0.25%
            } else {
                propertyTaxRate = 0.004; // 0.4%
            }

            // 누진 공제 적용 재산세 계산
            let propertyTaxBase = 0;
            if (taxableAmount <= 60000000) {
                propertyTaxBase = taxableAmount * 0.001;
            } else if (taxableAmount <= 150000000) {
                propertyTaxBase = taxableAmount * 0.0015 - 30000;
            } else if (taxableAmount <= 300000000) {
                propertyTaxBase = taxableAmount * 0.0025 - 180000;
            } else {
                propertyTaxBase = taxableAmount * 0.004 - 630000;
            }

            const urbanTax = propertyTaxBase * 0.14; // 도시지역분 14%
            const educationTax = propertyTaxBase * 0.2; // 지방교육세 20%
            const propertyTaxTotal = propertyTaxBase + urbanTax + educationTax;

            // 2. 종합부동산세 계산
            let comprehensiveTaxBase = 0;
            let comprehensiveThreshold = 0;
            let infoMessage = '';

            if (housingCount === 1) {
                comprehensiveThreshold = 1200000000; // 1주택 12억
                infoMessage = '1주택자 기본공제 12억원 적용';
            } else if (housingCount === 2) {
                comprehensiveThreshold = 900000000; // 2주택 9억
                infoMessage = '2주택자 기본공제 9억원 적용';
            } else {
                comprehensiveThreshold = 600000000; // 3주택 이상 6억
                infoMessage = '3주택 이상 기본공제 6억원 적용';
            }

            const comprehensiveTaxable = totalPublicPrice - comprehensiveThreshold;

            if (comprehensiveTaxable > 0) {
                // 종부세 과세표준
                const taxBase = comprehensiveTaxable;
                
                // 종부세율 (누진세율)
                if (housingCount === 1) {
                    // 1주택자
                    if (taxBase <= 200000000) {
                        comprehensiveTaxBase = taxBase * 0.006; // 0.6%
                    } else if (taxBase <= 400000000) {
                        comprehensiveTaxBase = taxBase * 0.008 - 400000;
                    } else if (taxBase <= 600000000) {
                        comprehensiveTaxBase = taxBase * 0.012 - 2000000;
                    } else if (taxBase <= 1200000000) {
                        comprehensiveTaxBase = taxBase * 0.014 - 3200000;
                    } else {
                        comprehensiveTaxBase = taxBase * 0.02 - 10400000;
                    }
                } else {
                    // 2주택 이상 (조정지역 중과)
                    if (regulated === 'yes') {
                        if (taxBase <= 200000000) {
                            comprehensiveTaxBase = taxBase * 0.012; // 1.2%
                        } else if (taxBase <= 400000000) {
                            comprehensiveTaxBase = taxBase * 0.016 - 800000;
                        } else if (taxBase <= 600000000) {
                            comprehensiveTaxBase = taxBase * 0.024 - 4000000;
                        } else if (taxBase <= 1200000000) {
                            comprehensiveTaxBase = taxBase * 0.030 - 7600000;
                        } else {
                            comprehensiveTaxBase = taxBase * 0.050 - 31600000;
                        }
                        infoMessage += ' (조정지역 중과 적용)';
                    } else {
                        // 일반지역
                        if (taxBase <= 200000000) {
                            comprehensiveTaxBase = taxBase * 0.008; // 0.8%
                        } else if (taxBase <= 400000000) {
                            comprehensiveTaxBase = taxBase * 0.012 - 800000;
                        } else if (taxBase <= 600000000) {
                            comprehensiveTaxBase = taxBase * 0.020 - 4000000;
                        } else if (taxBase <= 1200000000) {
                            comprehensiveTaxBase = taxBase * 0.025 - 7000000;
                        } else {
                            comprehensiveTaxBase = taxBase * 0.040 - 25000000;
                        }
                    }
                }
            } else {
                infoMessage = '공시가격 합계가 종부세 기준 미만으로 종부세가 부과되지 않습니다.';
            }

            const ruralTax = comprehensiveTaxBase * 0.2; // 농어촌특별세 20%
            const comprehensiveTaxTotal = comprehensiveTaxBase + ruralTax;

            // 총 보유세
            const totalTax = propertyTaxTotal + comprehensiveTaxTotal;

            // 결과 표시
            document.getElementById('totalTax').textContent = formatKoreanCurrency(totalTax);
            document.getElementById('propertyTaxBase').textContent = formatCurrency(propertyTaxBase);
            document.getElementById('urbanTax').textContent = formatCurrency(urbanTax);
            document.getElementById('educationTax').textContent = formatCurrency(educationTax);
            document.getElementById('propertyTaxTotal').textContent = formatCurrency(propertyTaxTotal);
            
            document.getElementById('comprehensiveTaxBase').textContent = formatCurrency(comprehensiveTaxBase);
            document.getElementById('ruralTax').textContent = formatCurrency(ruralTax);
            document.getElementById('comprehensiveTaxTotal').textContent = formatCurrency(comprehensiveTaxTotal);
            
            document.getElementById('taxInfo').textContent = infoMessage;

            // 종부세 섹션 표시/숨김
            const comprehensiveSection = document.getElementById('comprehensiveSection');
            if (comprehensiveTaxBase > 0) {
                comprehensiveSection.style.display = 'block';
            } else {
                comprehensiveSection.style.display = 'none';
            }

            // 결과 섹션 표시
            document.getElementById('resultSection').style.display = 'block';
            document.getElementById('resultSection').scrollIntoView({ behavior: 'smooth', block: 'nearest' });
        }

        // 계산기 리셋
        function resetCalculator() {
            document.getElementById('publicPrice').value = '';
            document.getElementById('totalPublicPrice').value = '';
            document.querySelector('input[name="housingCount"][value="1"]').checked = true;
            document.querySelector('input[name="regulated"][value="no"]').checked = true;
            document.getElementById('multiHouseSection').style.display = 'none';
            document.getElementById('resultSection').style.display = 'none';
            window.scrollTo({ top: 0, behavior: 'smooth' });
        }

        // 결과 공유
        function shareResult() {
            const totalTax = document.getElementById('totalTax').textContent;
            const propertyTax = document.getElementById('propertyTaxTotal').textContent;
            const text = `부동산계산하기에서 보유세를 계산했어요!\n재산세: ${propertyTax}\n연간 총 보유세: ${totalTax}`;
            
            if (navigator.share) {
                navigator.share({
                    title: '보유세 계산 결과',
                    text: text
                });
            } else {
                alert('결과가 클립보드에 복사되었습니다!');
                navigator.clipboard.writeText(text);
            }
        }
    </script>
</body>
</html>
